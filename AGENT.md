# 注意事项

1. 请严格按照要求进行开发，不要随意更改代码
2. 这是一个CLI工具，创建的所有测试文件都放在tests目录下
3. 这是一个CLI工具，temp目录下是临时文件，不要提交到git
4. 这是一个CLI工具，temp目录下是CLI创建的测试服务的目录
5. 这是一个CLI工具，在需要修改temp目录下的服务测试时，不要引用temp目录以外的代码
6. 这是一个CLI工具，你可以在temp目录中使用 `node ../dist/index.js init name` 创建主服务
7. 这是一个CLI工具，你可以在主服务目录中使用 `node ../dist/index.js init name -t ms` 创建微服务
8. 如果temp中的服务修改测试成功后，请将 `src/templates` 中的模板文件更新
9. 请全程使用`pnpm`进行包管理
10. 请按照下面的任务列表进行开发
11. 如果下面任务列表中的任务已经完成，请标记任务完成
12. 只开发下面未完成的任务
13. 请执行完一个任务再执行另一个任务
14. `local_modules` 和 `external_modules` 是两个不同的概念，`local_modules` 是本地模块，`external_modules` 是外部模块,但是它们的结构是一样的，用途也是一样的
15. 将所有的文档及代码注释使用英文编写
16. 当所有任务完成后，检查CLI及模板中功能的实现，对项目整体进行测验确保功能正常
17. 当所有任务完成后，重新编写README文档

# 任务列表

- [x] 一个主服务中包含多个微服务，一个微服务中也会包含多个微服务，层级关系是树形的，当我在任意层级的服务中运行pnpm i时，应该自动安装所有层级的服务的依赖
- [x] 在主服务中创建微服务时，应该自动配置端口映射
- [x] 在主/微服务运行时，我在主/微服务中创建微服务，主/微服务应该自动加载微服务并通过端口映射文件配置端口，我不要任何操作就可以访问新建的微服务
- [x] 在运行服务时，清理一下端口映射中的所有端口
- [x] 添加日志管理和日志聚合功能
- [ ] 实现服务发现和注册中心
- [ ] 将所有的文档及代码注释使用英文编写
- [ ] 添加CLI命令行帮助系统和版本信息
- [ ] 实现服务健康检查和监控功能
- [ ] 添加服务间通信和API网关功能
- [ ] 实现配置文件热重载功能